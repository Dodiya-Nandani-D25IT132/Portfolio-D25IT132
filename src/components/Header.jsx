function Header({ name }) {

    return (

        <header className="header">

            <h1>{name}</h1>

            <p>Student Portfolio using React</p>

        </header>

    );

}

export default Header;