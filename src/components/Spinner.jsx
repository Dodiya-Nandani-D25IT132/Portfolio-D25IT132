function Spinner() {
    return (
        <div className="spinner">
            <h2>Loading...</h2>
            <p>Please wait while repositories are loading.</p>
        </div>
    );
}

export default Spinner;